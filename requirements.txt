# HUD Notes v2.0.4 Requirements
# Core dependencies - minimal set that actually works

# Global hotkey support (required)
pynput>=1.7.6

# Markdown processing for templates and highlighting (required)
markdown2>=2.4.0

# GUI framework - tkinter usually comes with Python
# If missing on Linux: sudo apt install python3-tk

# That's it - keeping dependencies minimal
# Optional features removed to reduce complexity

# Known issues:
# - Preview feature broken with tab system
# - Multi-monitor support not working
# - Linux/X11 compatibility issues