# HUD Notes v2.1.0 Requirements

# Global hotkey support (required)
pynput>=1.7.6

# Markdown processing for templates and highlighting (required)
markdown2>=2.4.0

# tkinter is also required but comes with Python
# If missing on Linux: sudo apt install python3-tk
# If missing on macOS: brew install python-tk
